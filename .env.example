# Optional custom RPC endpoint. Keep actual credentials outside source control.
RPC_URL=https://sepolia.base.org
# Set only in your local terminal when deploying to a public network.
DEPLOYER_PRIVATE_KEY=
